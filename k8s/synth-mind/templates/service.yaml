apiVersion: v1
kind: Service
metadata:
  name: {{ include "synth-mind.fullname" . }}
  labels:
    {{- include "synth-mind.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "synth-mind.selectorLabels" . | nindent 4 }}
